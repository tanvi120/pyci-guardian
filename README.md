# PyCI Guardian 🛡️

A local AI-powered CI agent that:
- Reads git diff
- Reviews code (rule-based + Ollama)
- Runs tests
- Detects failures
- Suggests fixes
- Creates fix branches

## Run

```bash
pip install -r requirements.txt
python main.py
